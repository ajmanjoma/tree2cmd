name: Auto Version Bump

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  bump:
    if: |
      contains(github.event.head_commit.message, '#bump-patch') ||
      contains(github.event.head_commit.message, '#bump-minor') ||
      contains(github.event.head_commit.message, '#bump-major')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository (with full history + tags)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set Git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install bump2version
        run: pip install bump2version

      - name: Determine bump type & run bump2version
        id: bump
        run: |
          set -e
          MSG="${{ github.event.head_commit.message }}"

          echo "📝 Commit message received: $MSG"

          if echo "$MSG" | grep -q "#bump-major"; then
            echo "type=major" >> $GITHUB_OUTPUT
            bump2version major --allow-dirty --message "🔺 MAJOR: Version bumped to {new_version} [CI Auto]"
          elif echo "$MSG" | grep -q "#bump-minor"; then
            echo "type=minor" >> $GITHUB_OUTPUT
            bump2version minor --allow-dirty --message "🔸 MINOR: Version bumped to {new_version} [CI Auto]"
          else
            echo "type=patch" >> $GITHUB_OUTPUT
            bump2version patch --allow-dirty --message "🔹 PATCH: Version bumped to {new_version} [CI Auto]"
          fi

      - name: Push version bump commit + tags
        run: |
          git push
          git push --tags

      - name: Print bump summary
        run: |
          echo "🎉 Auto version bump complete!"
          echo "🔧 Type: ${{ steps.bump.outputs.type }}"
